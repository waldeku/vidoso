import { cva } from "class-variance-authority";

export const listStyles = cva(["flex-1", "h-[600px]", "overflow-y-auto"]);

export const loaderStyles = cva(["mt-24"]);

export const errorStyles = cva(["mt-24"]);
